

<h1>
   <%= @the_user.username %> 
</h1>
<dl>
  <dt>
    ID
  </dt>
  <dd>
    117
  </dd>

  <dt>
    Edit user
  </dt>
  <dd>
<form action="/update_user/<%= @the_user.id %>" method="post">
  <!-- Label for the username input -->
 <label for="username">Username:</label>
  <input id="username" type="text" name="query_username" value="<%= @the_user.username %>">
  <button type="submit"> Update user</button>
</form>

  </dd>
</dl>



<table border="1">
  <tbody>
    <tr>
      <th>ID</th>
      <th>Address</th>
      <th>Name</th>
      <th>Neighborhood</th>
      <th>Posted</th>
      <th></th>
    </tr>
    <% @the_user.commented_venues.each do |a_venue| %>
    <tr>
      
      <td><%= a_venue.id %></td>
      <td><%= a_venue.address %></td>
      <td><%= a_venue.name %></td>
       <td><%= a_venue.neighborhood %></td>
      <td><%= a_venue.created_at %></td>
      <td> <a href="/users/<%= a_venue.id %>">
          Show details
        </a></td>
    </tr>
    <% end %>
  </tbody>
</table>


</tbody>
</table>
